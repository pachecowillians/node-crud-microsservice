version: "3"
services:
    insert:
        image: pachecowillians/delete:latest
        ports:
            - "3334:3000"
        env_file:
            - ../.env
        deploy:
            replicas: 2
            placement:
                constraints: [node.role==worker]
